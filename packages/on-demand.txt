# The packages the HOST installs on first import, into the session's own
# site-packages. A block never installs anything: a sandbox that can reach an
# index is a sandbox that can write its own next payload, which is why the audit
# hook refuses a process and ctypes reaches no symbol.
#
# They are not in base.txt for one measured reason (darwin-arm64, installed size
# on disk, test suites stripped): these seven and their dependencies are 161 MB,
# against 21 MB for everything base.txt ships and 68 MB for the interpreter.
#
# `tabulate` is here for a different reason: the pandas SHIM renders through it,
# so shipping the real one would shadow a module the shim beside it still needs.
# It goes when pandas does.
#
# Their own dependencies resolve at install time; only the names a shim promises
# today are pinned here.
fonttools==4.64.0
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5
pillow==12.3.0
pytest==9.1.1
tabulate==0.10.0
python-pptx==1.0.2
